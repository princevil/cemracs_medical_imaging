function [pointer,pattern]=AIrestriction(m,k,points,N,D)
pointer = 2^N*(2.^(m-points.j).*points.k-k);
pattern = logical((-D*2^N<pointer)&(pointer<L*2^J));
pointer=pointer+L*2^J;
pointer=pointer(pattern);